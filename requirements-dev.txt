# requirements-dev.txt — Desarrollo y pruebas
# Instala el runtime del servidor + lo necesario para ejecutar TODA la suite
# (incluye las dependencias del motor cliente: Fase 1 y Fase 2).
# Instalar con: pip install -r requirements-dev.txt

-r requirements.txt

# Motor cliente (Fase 1 / Fase 2) — necesarias para sus tests
cryptography==42.0.8         # Cifrado Fernet + HMAC
psutil==5.9.8                # Lectura de MAC del servidor
httpx==0.27.0                # Cliente HTTP de validación online

# Testing
pytest==8.2.0
pytest-django==4.12.0
pytest-cov==5.0.0
